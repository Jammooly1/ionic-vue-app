import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jamal.vuememories',
  appName: 'Vue Memories',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
